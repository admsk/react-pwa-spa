--passo 1 - criar o projeto a partir do template
- yo @microsoft/sharepoint
- Escolher sharepoint online

- Deseja permitir ao administrador de inquilinos a opção de poder implantar a solução em todos os sites imediatamente?
- Os componentes da solução exigirão permissões para acessar APIs da web exclusivas e não compartilhadas com outras
omponentes no inquilino?

--passo 2 - Criar webpart de cadastro de Fornecedores

- Nome da webpart - CadastraFornecedores
- Descrição - WebPart que permite cadastrar um fornecedor

--passo 4 - instalar o pnp, forcei a versão porque essa esta homologada através do projeto CDM
npm install @pnp/logging@1.3.7 @pnp/common@1.3.7 @pnp/odata@1.3.7 @pnp/sp@1.3.7 --save

passo 5 - INSTALAR BOOTSTRAP, JQUERY E FONT AWESOME

npm install jquery bootstrap@4 --save
npm install @types/jquery @types/bootstrap@4 @fortawesome/fontawesome-free --save-dev

--instalar o url-loader com a versão indicada
npm install url-loader@2.2.0 --save-dev


--Criar lista Paises
Title	            Texto com uma linha
Ativo             boolean

--Criar lista Fornecedores
Title             Texto com uma linha
Pais	            Consulta(lookup) com lista paises
CNPJ          	  Texto com uma linha
Telefone          Texto com uma linha
Email             Texto com uma linha
Ativo             boolean

--Lista Endereço
Title	            Texto com uma linha
Cidade            Texto com uma linha
Estado            Texto com uma linha
IDFornecedor      Numero


--configurar o gulp para compilar font awesome
abra o aquivo gulpfile.js na raiz do projeto e substitua todo o código com o abaixo

//inicio do arquivo gulpfile.js
"use strict";

const gulp = require("gulp");
const build = require("@microsoft/sp-build-web");
build.addSuppression(
  `Warning - [sass] The local CSS class 'ms-Grid' is not camelCase and will not be type-safe.`
);
build.configureWebpack.mergeConfig({
  additionalConfiguration: generatedConfiguration => {
    generatedConfiguration.module.rules.push({
      test: /\.woff2(\?v=[0-9]\.[0-9]\.[0-9])?$/,
      use: {
        loader: "url-loader", query: { limit: 10000, mimetype: 'application/font-woff2'}
      }
    });
    return generatedConfiguration;
  }
});
build.initialize(gulp);
//fim do arquivo gulpfile.js


--descompactar o zip disponibilizado como recurso nesse video e copiar
a pasta chamada stylelibrary e colar dentro da pasta src do seu projeto


--criação de pastas






--na sua webpart que queira usar o pnp, jquery, font awesome, toast
--faça os imports abaixo do arquivo de strings

import { sp, ItemAddResult } from "@pnp/sp";
import * as $ from "jquery";
import "bootstrap";

require('../../../node_modules/bootstrap/dist/css/bootstrap.min.css');
require('../../../node_modules/@fortawesome/fontawesome-free/css/all.min.css');

//css padrao
require('../../stylelibrary/css/padrao.css');

//importa toast
require('../../stylelibrary/css/toastr.min.css');
import toastr from '../../stylelibrary/js/toast/toastr.min.js';



-- colar acima do render o metodo que configura o pnp
public onInit(): Promise<void> {

    return super.onInit().then(_ => {
      sp.setup({
        spfxContext: this.context
      });
    });
  }

--substitua seu metodo render por este
  public render(): void {

    //carrego o template de layout
    this.domElement.innerHTML = require("./template.html");

    document
      .getElementById("btnSalvar")
      .addEventListener("click", (e: Event) => this.SalvarAniversariante());

    this.ObterTodasAreas();
  }



--cole esses scripts abaixo do render
  public async ObterTodasAreas() {

    let montahtmlAreas;

    await sp.web.lists
      .getByTitle("Areas2")
      .items.top(5000)
      .select("ID, Title")
      .orderBy("Title",true)
      .get()
      .then(items => {

        montahtmlAreas = `<option value="">Selecione</option>`;
        items.forEach(element => {
          montahtmlAreas += `<option value="${element.Title}">${element.Title}</option>`;
        });

        $("#ddlArea").html(montahtmlAreas);

      })
      .catch(e => {
        console.log("erro", e);
      });
  }

  protected SalvarAniversariante(): void {

    toastr.options = {
      "closeButton": false,
      "debug": false,
      "newestOnTop": false,
      "progressBar": true,
      "positionClass": "toast-top-center",
      "preventDuplicates": false,
      "onclick": null,
      "showDuration": "300",
      "hideDuration": "1000",
      "timeOut": "4000",
      "extendedTimeOut": "1000",
      "showEasing": "swing",
      "hideEasing": "linear",
      "showMethod": "fadeIn",
      "hideMethod": "fadeOut"
    };

    let Nome = $("#txtNome").val();
    let DataAniversario = new Date(`${$("#txtDataAniversario").val()}T12:00:00`).toISOString();
    let Area = $("#ddlArea option:selected").text();
    let UrlFoto = $("#txtURLFoto").val();
    let Observacao = $("#txtObservacao").val();

    sp.web.lists.getByTitle("Aniversariantes2").items.add({
      Title: Nome,
      DataAniversario: DataAniversario,
      Area: Area,
      UrlFoto: UrlFoto,
      Observacao: Observacao
    }).then((iar: ItemAddResult) => {
      toastr["success"]("Cadastro realizado com sucesso!", "Sucesso");
    },
      (err) => {
        console.log(err);
        toastr["error"]("Ocorreu um erro ao realizar o cadastro.", "Erro");
      });
  }


--crie um aquivo chamado template.html na raiz da pasta da webpart e cole o código abaixo


/*inicio do codigo do arquivo template.html*/

<section id="formAniversariante" class="formPadrao">
  <div class="row">
    <div id="Tabs" class="col-md-12">
      <ul id="tabsJustified" class="nav nav-tabs">
        <li id="tabDetalhes" class="nav-item"><a href="#" data-target="#Detalhes" data-toggle="tab" class="nav-link small text-uppercase active">Detalhes</a></li>
      </ul>
      <div id="tabsJustifiedContent" class="tab-content">
        <div id="Detalhes" class="tab-pane fade show active">
          <div class="row" id="divPrincipal">
            <div class="col-md-1 labelDesc">
              <label class="control-label">Nome:</label></div>
            <div class="col-md-5 ">
              <input type="text" id="txtNome" class="form-control mb-4" placeholder="Nome do aniversariante">
            </div>
            <div class="col-md-1 labelDesc">
              <label class="control-label">Aniversário:</label>
            </div>
            <div class="col-md-5 ">
              <input type="date" class="form-control" id="txtDataAniversario" placeholder="">
            </div>
            <div class="col-md-1 labelDesc">
              <label class="control-label">Área:</label>
            </div>
            <div class="col-md-3 ">
              <select id="ddlArea" class="browser-default custom-select mb-4">
              </select>
            </div>
            <div class="col-md-1 labelDesc">
              <label class="control-label">Foto:</label></div>
            <div class="col-md-7 Componentes">
              <input type="text" id="txtURLFoto" class="form-control mb-4" placeholder="Url da foto">
            </div>
          </div>
          <div class="row">
            <div class="col-md-12 Componentes-MultLine"><textarea class="form-control" id="txtObservacao" rows="3"
                placeholder="Observação"></textarea></div>
          </div>

          <div class="row rodapeForm" id="divBotoes">
            <button id="btnSalvar" class="btn btnPadrao" type="submit"><i class="far fa-save"></i>
              Salvar</button>
          </div>
        </div>

      </div>
    </div>
  </div>
  </div>
  </div>
</section>



https://avatars1.githubusercontent.com/u/46784618?s=460&u=925b47a59ce067cf1c2f1d3d448fdf5401ed845e&v=4
